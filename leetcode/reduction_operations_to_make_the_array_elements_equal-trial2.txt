class Solution:
    def reductionOperations(self, nums: List[int]) -> int:
        nums.sort()
        n = len(nums)
        operations = 0

        for i in range(n-1,0,-1):
            if nums[i-1] != nums[i]:
                operations += (n-i)

        return operations


